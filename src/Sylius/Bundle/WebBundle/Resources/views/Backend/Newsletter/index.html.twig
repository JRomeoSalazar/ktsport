{% extends 'SyliusWebBundle:Backend:layout.html.twig' %}

{% import 'SyliusResourceBundle:Macros:buttons.html.twig' as buttons %}
{% from 'SyliusWebBundle:Backend/Macros:misc.html.twig' import pagination %}
{% from 'SyliusWebBundle:Backend/Newsletter:macros.html.twig' import list %}

{% block topbar %}
<ol class="breadcrumb">
    <li>{{ 'sylius.breadcrumb.assortment'|trans }}</li>
    <li>{{ 'sylius.breadcrumb.newsletter.index'|trans }}</li>
</ol>
{% endblock %}

{% block content %}
<div class="page-header">
    <div class="actions-menu">
        {{ buttons.create(path('sylius_backend_newsletter_create'), 'sylius.newsletter.create'|trans) }}
        {{ buttons.manage(path('sylius_backend_newsletter_index'), 'sylius.newsletter.manage'|trans) }}
    </div>
    <h1><i class="glyphicon glyphicon-folder-close"></i> {{ 'sylius.newsletter.index_header'|trans|raw }}</h1>
</div>

{{ pagination(newsletters) }}
{{ list(newsletters) }}
{{ pagination(newsletters) }}

{% endblock %}
