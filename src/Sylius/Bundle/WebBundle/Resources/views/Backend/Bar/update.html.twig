{% extends 'SyliusWebBundle:Backend:layout.html.twig' %}

{% from 'SyliusResourceBundle:Macros:actions.html.twig' import update %}

{% block topbar %}
<ol class="breadcrumb">
    <li>{{ 'sylius.breadcrumb.configuration'|trans }}</li>
    <li><a href="{{ path('ktsport_backend_bar_index') }}">{{ 'sylius.breadcrumb.bar.index'|trans }}</a></li>
    <li><a href="{{ path('ktsport_backend_bar_show', {'id': bar.id}) }}">{{ bar.title }}</a></li>
    <li>{{ 'sylius.breadcrumb.edit'|trans }}</li>
</ol>
{% endblock %}


{% block content %}
<div class="page-header">
    <h1><i class="glyphicon glyphicon-pencil"></i> {{ 'sylius.bar.update_header'|trans|raw }}</h1>
</div>

{{ form_errors(form) }}

<form action="{{ path('ktsport_backend_bar_update', {'id': bar.id}) }}" method="post" class="form-horizontal" novalidate>
    <input type="hidden" name="_method" value="PUT">
    {% include 'SyliusWebBundle:Backend/Bar:_form.html.twig' %}
    {{ update() }}
</form>
{% endblock %}